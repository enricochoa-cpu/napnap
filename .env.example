# Supabase Configuration
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Sentry Configuration (optional - for error tracking)
# Get this from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Cursor MCP – Supabase (optional, for AI database tools in Cursor)
# Get PAT: Supabase Dashboard → Settings → Access Tokens → Generate
# Project ref: the subdomain of your project URL (e.g. abc123 from https://abc123.supabase.co)
# SUPABASE_ACCESS_TOKEN=sbp_xxxxx
# SUPABASE_PROJECT_REF=your-project-ref

# Supabase Edge Function Secrets (set via `npx supabase secrets set`)
# These are NOT frontend env vars — they run server-side in Edge Functions.
#
# RESEND_API_KEY=re_xxxxx
#   Get from: https://resend.com → Dashboard → API Keys → Create API Key
#
# APP_URL=https://your-app-url.com
#   The public URL of your app (used in invitation email CTA links)
