# 2026-03-01 — About section and app version in Support

## Summary

Added an **About** screen under Support (Profile → Support → About) that shows app name (NapNap), release version, and links to Terms of Service and Privacy policy. Terms and Privacy were removed from the Support menu so they are only reachable via About, keeping Support focused and matching common app patterns.

## Changes

### Version in build
- **vite.config.ts**: Reads `version` from `package.json` and defines `__APP_VERSION__` at build time so the UI can display the release version.
- **src/vite-env.d.ts**: Declares `__APP_VERSION__` for TypeScript (and references `vite/client`).

### About view
- **src/components/Profile/AboutView.tsx** (new): SubViewHeader, app info card (NapNap + “Version X.X.X” from `__APP_VERSION__`), and Legal section with buttons that navigate to Terms of Service and Privacy policy. Back returns to Support.

### Support and routing
- **ProfileMenu.tsx**: Added `'about'` to `ProfileView` type.
- **SupportView.tsx**: Added “About” as the first row (icon + “App version and legal” subtitle). Removed Terms of Service and Privacy policy rows; those are now only in About.
- **ProfileSection.tsx**: Import `AboutView`, `handleBackFromAbout` (back to Support), and render `AboutView` when `currentView === 'about'`. Terms/Privacy back navigation still uses `previousView`, so returning from Terms/Privacy from About goes back to About.

### i18n
- **en, es, ca**: New `support.about`, `support.aboutMenuSubtitle` and full `about` block: `title`, `subtitle`, `appName`, `version`, `legal`, `termsOfService`, `privacyPolicy`.

## Scroll fix (My Babies / Stats in Chrome)

Scroll did not work on My Babies and Stats in Google Chrome (desktop and Android); Cursor browser was fine. Cause: `main` had overflow (hidden or overflow-x-hidden), which made it a scroll container; Chrome miscalculates height when that container has a transformed child (Framer Motion slide). Fix: added an **inner scroll container** inside `main` — a div with `height: 100dvh`, `overflow-y: auto`, `overflow-x: hidden`, and a ref. Scroll now happens inside this div in all browsers. Tab change and Profile sub-view change reset scroll via the ref and `onScrollToTop` callback to ProfileSection. Documented in lessons.md §6.7.

### Files
- **App.tsx**: Removed overflow from `main`; added inner div with `mainScrollRef`, `scrollMainToTop` callback, scroll reset in `handleViewChange`, and `onScrollToTop={scrollMainToTop}` passed to ProfileSection.
- **ProfileSection.tsx**: Optional `onScrollToTop` prop; called in useEffect when `currentView` changes so profile sub-views reset the main scroll container.
- **.context/lessons.md**: New §6.7 (scroll not working on My Babies/Stats in Chrome).

## Share Access as dedicated screen + nav bar tap behaviour

### Share Access screen
- **ShareAccessView.tsx** (new): Full-screen view with SubViewHeader (“Share baby profile” / “Compartir perfil del nadó” / “Compartir perfil del bebé”) and existing `ShareAccess` component. Replaces the inline Share Access block that lived inside Baby Detail.
- **BabyDetailView.tsx**: Removed embedded `ShareAccess`. Owners see a single “Share baby profile” row that navigates to the new Share Access screen. Props simplified: no longer receives `myShares`, `onInvite`, `onUpdateRole`, `onRevokeAccess`, `inviterName`; added `onOpenShareAccess`.
- **ProfileSection.tsx**: New view `'share-access'` in routing. From Baby Detail, “Share baby profile” → ShareAccessView; back → Baby Detail. ShareAccessView receives baby, myShares, invite/role/revoke callbacks, inviterName.
- **ProfileMenu.tsx**: Added `'share-access'` to `ProfileView` type.
- **app_flow.md**: Baby Detail sections updated; new §2.4.2b Share Access.
- **i18n**: `shareAccess.title` and `babyDetail.manageSharing` set to “Share baby profile” / “Compartir perfil del nadó” / “Compartir perfil del bebé” (en, ca, es).

### Nav bar: tap tab = go to that screen
- Tapping any nav icon (Today, History, Stats, Profile) always switches to that tab. When already inside Profile (e.g. Baby Detail) and user taps the Profile icon, the app resets to the Profile menu so one tap reaches “Account settings” / “My babies” without tapping back twice.
- **App.tsx**: `profileTabTapCount` state; Profile tab button increments it and calls `handleViewChange('profile')`.
- **ProfileSection.tsx**: New prop `resetToMenuTrigger` (number). When it changes, `useEffect` sets `currentView` to `'menu'` and clears `selectedBabyId`.

## Verification

- `npm run build` passes.
- Support menu: About, FAQs, Contact, then tip card. Terms and Privacy only via About → Legal.
- My Babies and Stats scroll correctly in Chrome (desktop and Android).
- Share Access is a dedicated screen from Baby Detail → “Share baby profile”. Back returns to Baby Detail.
- From Baby Detail (or any Profile sub-view), tapping the Profile tab icon goes to the Profile menu.
